<h1> Portfolio Items </h1>

<% @portfolio_item.each do |x| %>
 <p> <%= x.title %></p>
 <p> <%= x.subtitle %></p>
 <p> <%= x.body %></p>
 <%= image_tag x.thumb_image unless x.thumb_image.nil? %>

<% end %>
